# v4实验注册表：不同周期和多周期信号

> 状态：已锁定，创建于2026-09-02，早于任何v4回测结果  
> 适用输出：首个通过v3复现闸门的正式v4时间戳目录  
> 研究边界：只执行v4，不执行v5，不修改任何v2/v3配置、代码、缓存或输出

## 1. 冻结的执行基准

所有v4场景完整继承v3正式基准：客户手续费=逐合约历史交易所标准×1.5；分品种正常滑点；换月每腿额外1 tick；滞后成交量参与率冲击；混合周度；120%组合实现波动率紧急减仓；10% buffer；目标年化波动率27.5%；商品保证金、商品杠杆、国债豁免、流动性、主力映射和Panama规则均不变。信号在t日收盘形成，订单在下一交易日开盘执行。

## 2. 信号及缺失规则

原始信号均使用Panama复权收盘价的绝对点差，不使用百分比收益：

```text
Score(h, skip) = mean(ΔP[t-skip-h+1 : t-skip])
                 / std(ΔP[t-skip-h+1 : t-skip], ddof=1) × sqrt(252)
```

- 20、60、120、180、250日窗口必须分别具备完整20、60、120、180、250个点差观测；
- skip=5时仍要求完整h个点差，只是窗口结束于t-5；
- “12个月减最近1个月”固定为skip=20、有效窗口230个点差，近似覆盖t-250至t-20；不是两个Sharpe相减；
- 任一所需周期缺失时，多周期组合整体缺失，不对剩余周期做动态再加权；
- 不回填未来值，不使用全样本统计量补足历史。

## 3. Forecast scaling预注册口径

- 目标平均绝对forecast：10；
- 对每个周期，先计算当日所有可得品种原始Score的截面平均绝对值；
- t日scale只使用截至t-1日的上述序列的扩展历史均值；
- 至少需要126个有效历史交易日，否则scale和scaled forecast保持缺失；
- `scale = clip(10 / lagged_expanding_mean_abs_score, 0.1, 100)`；
- `scaled_forecast = clip(raw_score × scale, -20, 20)`；
- scale按周期共享于当日各品种，不根据品种收益、策略盈亏或未来数据估计；
- 多周期名义权重始终等权，不优化。

## 4. 固定实验顺序

### 阶段0：v3复现闸门

1. `reference_v3_252`：用v4信号代码计算252日、skip=0原始Score。

必须与`outputs/v3_20260902_001129/00_formal__formal_baseline`比较每日权益、持仓、成交手数、手续费、滑点和期末权益。持仓及成交分类要求完全一致；金额误差容许浮点误差不超过0.01元。未通过则停止，不运行后续场景。

### 阶段1：普通单周期研究候选

2. `single_20`  
3. `single_60`  
4. `single_120`  
5. `single_180`  
6. `single_250`

### 阶段2：短期反转处理研究候选

7. `single_20_skip5`  
8. `single_60_skip5`  
9. `single_120_skip5`  
10. `single_180_skip5`  
11. `single_250_skip5`  
12. `single_250_minus_20`：skip=20、有效窗口230。

### 阶段3：固定等权多周期研究候选

13. `multi_fast_raw`：20、60、120，原始Score等权  
14. `multi_fast_scaled`：20、60、120，scaled forecast等权  
15. `multi_all_raw`：20、60、120、250，原始Score等权  
16. `multi_all_scaled`：20、60、120、250，scaled forecast等权  
17. `multi_slow_raw`：60、120、180、250，原始Score等权  
18. `multi_slow_scaled`：60、120、180、250，scaled forecast等权

### 阶段4：多周期边际盈亏诊断（不参与候选排名）

对上述每个多周期组合，分别删除其中一个周期后完整回测，边际净利润定义为：

```text
该周期边际净利润 = 完整组合净利润 - 删除该周期后的组合净利润
```

共22个预注册留一法场景：快速组合每版本3个、全周期每版本4个、慢速组合每版本4个，raw/scaled分别执行。留一后仍使用原组合对保留周期的等权规则；scaled版本沿用同一因果scale算法。该诊断不用于扩大候选集合或优化权重。

### 阶段5：最多三个方案的固定滑点压力复核

压力候选只从阶段1—3的17个研究候选中按第5节规则自动选出，最多三个。每个入选方案只运行固定2 tick和固定3 tick两个压力场景；手续费、混合周度、buffer等其余条件不变。不得临时增加第四个候选或其他tick。

## 5. 候选评价与压力筛选规则

### 5.1 硬性评价项

每个候选计算以下七项是否通过：

1. 样本内和验证期年化收益均大于0；
2. 2020年至样本终点的滚动下一年拼接年化收益大于0；
3. 同时删除2020和2024后，全样本年化收益与Sharpe均大于0；
4. 最大回撤不高于v3的绝对值，或Calmar至少比v3提高5%；
5. 年化换手和总交易成本均不超过v3的125%；
6. 正盈利年份前两名利润占比不高于v3；
7. FG占化工能源正利润之比不高于v3，且单一板块占正板块利润之比不高于v3。

### 5.2 固定排序

候选先按通过项总数降序，再依次按验证期Sharpe、删除2020/2024后Sharpe、Calmar降序，最后按年份前两名利润占比和年化换手升序。不得使用全样本收益作为首要排序项。

### 5.3 最多三个压力候选

- 先从“普通/跳过近期单周期”“raw多周期”“scaled多周期”三个家族各取排序最高且通过硬性项1—3的一个；
- 某家族没有合格者时，不强行占位；剩余额度按全体固定排序补足，但总数不超过3；
- v3参照组和阶段4留一法诊断不参加筛选。

## 6. 最终替换v3的证据门槛

只有候选同时满足以下条件才可推荐替换v3：

- 硬性项1—3全部通过；
- 年份前两名利润集中度严格低于v3，且FG依赖不高于v3；
- 最大回撤不高于v3，或Calmar至少提高10%；
- 固定2 tick及3 tick压力下样本内和验证期收益均为正；
- 单周期候选的相邻窗口验证期收益方向一致；多周期候选至少有同一家族raw/scaled两个版本验证期均为正；
- 成本和换手不超过v3的125%。

若没有方案满足全部替换门槛，最终结论必须是“v4没有足够证据替换v3信号”，继续保留v3的12个月方案。不得为了进入v5强行选择。

## 7. 预注册输出

所有场景均保存完整权益、持仓、目标、订单、成交、拒单和逐品种盈亏CSV/pickle。另输出场景指标、年度及滚动结果、稳健性、集中度、板块/品种/多空贡献、成本、buffer、保证金、forecast分布/相关性/scaling/有效贡献权重、留一法边际盈亏、v3复现核对、选择评分和压力结果。2026年必须标记不完整年度。

本注册表锁定后只允许修复会导致公式、时序、记账或程序运行错误的问题；任何修复必须在审计报告中记录，不能借修复新增有利参数。
