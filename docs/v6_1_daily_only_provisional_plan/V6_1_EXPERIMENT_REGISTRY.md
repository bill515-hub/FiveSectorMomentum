# v6.1 实验注册表（绩效结果前冻结）

状态：**LOCKED_BEFORE_PERFORMANCE**  
证据等级：**PROVISIONAL_DAILY_ONLY**

本注册表只允许执行 `V6_1_MACHINE_REGISTRY.yaml` 中 sequence 1—14。两个策略固定为 `reference_v3_252` 与 `sleeve_20skip5_250_equal_risk`；完整历史 attempt 上限16次。运行顺序及闸门为：R01/R02 → P01/P02 → P03/P04 → P05/P06 → P07/P08 → P09/P10 → P11/P12。

R组只验证旧路径兼容性，保留历史 `/10000` 费率缺陷，不能作为修正经济结论。P/S/C组固定使用 `/1000` 比例费率、T固定费特例、1.5倍客户费率以及开仓、平昨、平今分类。P1是正式日线代理，P0/P2是代理区间，固定1/3 tick和next-close只作敏感性。

`high==low` 仅是一价日形态代理，只能作用于引擎实际订单的真实合约与换月腿。它不是实际涨跌停证据；挑战集也不是真值标签。供应商逐日保证金未经交易所规则核验，缺失时使用冻结静态fallback×1.25，均不得描述成官方历史保证金。

不得按结果新增场景、调整参数或改变代理解释。任何强制闸门失败即停止。
