# V6.2 Gate Errata v1.1（结果前恢复修订）

状态：`LOCKED_BEFORE_ANY_SAVED_V6_2_PERFORMANCE`  
授权：用户于 2026-09-06 明确要求按阻断报告建议执行下一轮任务。

## 1. 适用范围

本勘误只修复 R01 attempt 1 暴露出的两个非经济实现缺陷。`V6_2_MACHINE_REGISTRY.yaml`、resolved config、12 个场景、2 个策略、单因素比较边和 14 次 attempt 上限均保持不变。

R01 attempt 1 没有保存绩效表，逐表复现闸门尚未评价；因此本勘误仍发生在任何可用 v6.2 新绩效产生之前。

## 2. E01：事后一价形态诊断字段规范化

规范化 bundle 的日线合约键是 `ts_code`，而订单/成交键是 `contract`。事后诊断必须把日线 `ts_code` 明确重命名为内部 `contract` 后再连接。

此修复不得：

- 进入订单接受或成交数量逻辑；
- 改变任何订单、成交、持仓、权益或成本；
- 将 `high==low` 描述为官方限价真值。

新增真实 bundle schema 回归测试，确保生产日线字段可连接且仅生成事后诊断。

## 3. E02：attempt 与场景 sequence 状态机分离

attempt 编号按每次完整历史启动全局递增；场景 sequence 按已 `COMPLETED` 的场景数递增。规则锁定为：

1. 所有失败 attempt 永久占用额度，不返还；
2. 当前不存在 `STARTED` 未终结记录时才允许新启动；
3. 下一个合法场景 sequence = 已完成场景数 + 1；
4. 同一场景在尚未完成时允许消耗储备重试；
5. 同一场景一旦完成，禁止再次运行；
6. 全局 attempt 仍不得超过 14。

新增失败后同 sequence 重试、已完成重复禁止、跨 sequence 顺序和上限测试。

## 4. 算术勘误

attempt 1 已失败。若 R01 重试及其余 11 个场景此后全部一次通过，总使用量为 13/14，仍保留 1 次失败储备。上一轮 blocker 所称“没有额外失败余量”不正确，以本勘误为准。

## 5. 重新进入条件

完成 E01/E02 后必须重跑 G0—G3、旧路径哈希和 secrets 扫描。全部通过后才可用 attempt 2 重试 R01；随后仍严格按原 sequence 运行。任一新闸门失败立即停止。
