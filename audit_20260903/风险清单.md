# 五板块动量回测框架 — 风险清单（按严重程度排序）

> 审计范围：`src/five_sector_momentum/`（v1/v2 基础引擎 → v3 → v4 → v4_1 → v4_2 全部版本）+ 下载/费用脚本 + 配置。
> 审计日期：2026-09-03（本清单与《审计报告.md》配套）。**未改动任何已有代码、结果与文档。**
> 严重程度：高（可能产生静默错误结果或阻断运行）＞ 中（方向性/方法学偏差或数据缺口）＞ 低（可维护性/潜在隐患）。

| # | 严重程度 | 类别 | 位置（文件:行号） | 问题描述 | 整改建议 |
|---|:---:|:---:|---|---|---|
| 1 | 高 | 数据→合约经济参数 | `universe.py:74-77`、`engine.py:645-648`、`engine.py:257/263` | 配置内品种若不在 `FALLBACK_ECONOMICS` 且 `fut_basic` 也未提供乘数/最小变动价，`_fallback()` 静默回退到 `(point_value=1, tick=1, margin=0.18)`，导致该品种的 CNY 盈亏、波动、滑点金额错误达数量级，且无任何告警。 | 在数据构建阶段断言每个配置品种都有经济参数（来自 `fut_basic` 或 `FALLBACK_ECONOMICS`），缺失即 `raise`；`_fallback()` 改为显式抛错而非静默默认值。 |
| 2 | 中 | 交易成本（高估） | `engine.py:559-569`、`configs/five_sector_momentum.yaml:74`（`fallback_commission_rate: 0.0002`） | v1/v2 手续费兜底 `max(reported*1.2, 名义×2bp)`：对国债期货 T（名义≈100万/手）兜底≈**200元/手**，而实际 T 交易所费≈3元/手，高估约 60 倍；对 RB（名义≈3万）兜底≈6元 vs 实际≈3.6元，高估近 2 倍。兜底成为实际收费下限，v1/v2 的 T 腿成本被系统性高估（方向是保守，但数量级错误）。v3+ 已改用 tushare 规则×1.5，不再受影响。 | 兜底费率按品种/交易所区分；对固定每手费用品种（T/SC 等）禁用“名义费率兜底”，改为固定每手上限；或直接废弃 v1/v2 成本模型。 |
| 3 | 中 | 交易成本（健壮性） | `data_source.py:83-86`、`costs_v3.py:41`、`costs_v3.py:224` | `fut_settle` 被作为 `optional=True` 下载，首个批次失败即整端点禁用；v3+ 费用模型只保留 `source_status=="tushare"` 行，若规则表为空，引擎首次收费时 `_lookup` 抛 `KeyError`，**无降级路径**，直接阻断回测。 | `build_fee_rules` 输出为空时回退到 v1/v2 式固定费率并写入显式告警，而不是在引擎内崩溃。 |
| 4 | 中 | 方法学（样本外纯度） | `workflow_v4.py`（候选压力/推荐逻辑）、`analytics_v4_1.py:265`（自注“未做正式多重比较推断”）、`reports_v4_1/reports_v4_2`（按验证期 Sharpe 选 sleeve） | 2022-01-01 后的“验证期”被反复用于 21 个候选的排名/择优，等价于测试集被窥探，报告的验证期 Sharpe 存在乐观偏差与多重比较风险（框架已自我披露，但仍是方法学瑕疵）。 | 增设一个真正冻结的样本外期（结果只读一次），或对 21 策略比较做 Bonferroni / Westfall–Young 校正，并将“推荐”结论明确降级为探索性。 |
| 5 | 中 | 数据完整性 | `data_source.py:74-115`（下载无日历核对）、`data_pipeline.py:329-339`（缺 bar 直接 `continue`）、`data_source.py:117-150`（repair 仅补整段缺失合约） | 主下载路径不对交易日历做行数/连续性断言；合约内部单日缺失会被静默当成 NaN 并在 Panama 拼接时跳过，可能影响换月与信号；`repair_missing_bars` 只修复“整段缺失的合约”，不修复合约内的单日缺口。 | 下载/构建后调用交易日历核对（复用 v4_2 `TradingCalendarV42.audit_observed_dates`），对缺口断言或生成修复清单。 |
| 6 | 低 | 数据可靠性 | `download_v3_fee_data.py`（主循环单次 try） | 费用下载脚本未复用 `data_source._call` 的 4 次指数退避重试；失败合约写入 `download_failures.csv` 后**永不重采**，且无续传重试逻辑。 | 复用 `_call` 的退避重试；失败合约进入重采队列，脚本支持 resume。 |
| 7 | 低 | 未来函数（潜在） | `data_pipeline.py:372-379`（`_next_contract_by_segment`） | `FORWARD` 字段通过反向扫描整条换月序列确定“下一合约”，属于用未来换月信息标注字段；当前仅写入 `multiple_prices` 报表，**未进入信号/引擎**，无交易影响，但属潜在地雷。 | 在字段/文档中显式标注“含未来信息，禁止入信号”；或改为仅基于已知换月日的保守计算。 |
| 8 | 低 | 可维护性 | `engine_v4.py:61-78` vs `engine_v4_1.py:221-235`（重复）、`workflow_v4_1.py:9`（`import *`）、`workflow_v3.py`/`workflow_v4.py`（硬编码历史输出目录）、`workflow_v4_1.py`/`analytics_v4_2.py`（硬编码 split 日期） | 版本化复制粘贴（5 套 engine、5 套 workflow、4 套 analytics、4 套 reports）；通配导入污染命名空间；历史输出目录与样本切分日期硬编码，换环境/换日期即无法复现。 | 抽取公共执行内核与常量；改为显式 import；路径/日期统一从 `settings` 读取。 |
| 9 | 低 | 健壮性（掩盖） | `engine.py:247/293/534/554/591`、`data_pipeline.py:335` 等 `if isinstance(x, DataFrame): x.iloc[-1]` | 这些 `iloc[-1]` **不是未来函数**（均为取序列末行或对唯一索引的防御性去重），但若 `(date, ts_code)` 意外出现重复会静默取最后一行，掩盖数据重复问题。 | 数据层保证唯一后，改为断言唯一（`assert len(x)==1`）或显式 `.squeeze()`，让重复数据在源头暴露。 |
| 10 | 低 | 指标精度 | `analytics.py:31`、`analytics_v4.py:36` | 年化年限用 `len(frame)/252` 估算；中金所年交易日约 243，252 会轻微低估年限、高估年化收益/波动。 | 按实际交易日历（243/244）或逐日历年度实际交易日数计算。 |
| 11 | 低 | 可复现性 | `engine_v4.py:61-63`（注释）、`engine_v3.py` | v3 结果数值确定，但 `set` 迭代导致行序随进程变化；v4 在输出层规范化了排序，v3 未做。若需字节级复现，v3 原始文件行序不稳。 | 在 v3 输出前同样加 canonical sort。 |

## 结论性说明（未来函数专项）

- `shift(-1)`：全库 **0 处**（grep `shift\(\s*-` 无匹配）。
- 全量数据 `fit_transform` / sklearn 缩放：**0 处**（grep `fit_transform|\.fit\(|transform\(` 仅命中 2 处 groupby 报表聚合，非特征标准化）。
- `iloc[负索引]`：35 处，全部为“取序列末行”或“唯一索引防御性去重”，**无未来函数**。
- 唯一需要长期警惕的是第 7 条的 `FORWARD` 字段（未来换月身份，当前仅报表用途）。
