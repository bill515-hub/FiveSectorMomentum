# v4.2 实验注册表：20日跳5日—250日固定风险袖套

注册时间：2026-09-03（在本轮新增测试、回测和统计结果产生之前）。

唯一研究规范：`docs/NEXT_RESEARCH_PLAN_20SKIP5_250_SLEEVE_AND_DRAWDOWN_CORRELATION.md`，以该文件为准。本表锁定设计，不新增参数。输出根目录：`outputs/v4_2_20260903_072724`。本表保存后锁定，结果与状态另存，不回写注册设计。

## 1. 固定执行顺序与停止规则

先完成旧架构只读检查，运行现有 v2—v4.1 测试及新增的历史追加不变性前置检查；通过前置检查后，严格顺序运行下表。新增完整历史引擎运行最多4次。小样本自动化测试、纯分析和Bootstrap不是新增历史场景。

| 顺序 | 编号 | 唯一场景 | 成本 | 角色 |
|---|---|---|---|---|
| 1 | G1 | single_20_skip5 | C3正式 | 与原v4逐表复现 |
| 2 | G2 | single_250 | C3正式 | 与原v4逐表复现 |
| 3 | S1 | strategy_sleeve_20skip5_250_equal_risk | C3正式 | 唯一新增投资方案 |
| 4 | S2 | strategy_sleeve_20skip5_250_equal_risk_fixed_3tick | 固定3 tick | 同一方案成本压力 |

G1源：`outputs/v4_20260902_102628/02_single_skip__single_20_skip5`；G2源：`outputs/v4_20260902_102628/01_single_normal__single_250`。正式v3参照只读引用同目录的`00_reference__reference_v3_252`，不是250日袖套的组成部分。

闸门比较scores、selections、directions、targets、positions、orders、fills、daily_equity、成本、板块与品种盈亏；离散字段与行键一致，关键金额误差≤0.01元。失败不得以放宽容差、删行或改变参数绕过。

任何复现失败、账户不平、未来数据改变过去结果、虚拟目标与真实净订单成本无法区分，或关键数据不能恢复，立即停止研究，生成停止报告。未执行的闸门标记“未运行”，不得标记通过。发现缺陷后不在本轮静默修改历史引擎继续研究。

前置追加检查固定使用一个人工可复核周中截断样本，并以2016-01-06至2016-01-08作为真实缓存的周中/周末对照；两个信号均仅改变可见样本末端，过去价格不变。选择该截点是为了检验周度日历边界，不是择优选择回测参数。前置检查不是第五个投资实验。

## 2. 数据、信号、账户及成本锁定

- 只读复用`data/normalized_v2`及v3历史费用规则，不下载、不补写缓存。
- 2015-01-01至2026-08-31；预热自2014-01-01；样本内2015—2021，验证期2022起；2026是不完整年度。初始资金1,000万元。
- 快周期：20个完整绝对点差观测、跳过最近5个交易日；慢周期：250个完整观测、不跳过。Score=窗口内点差均值/样本标准差×√252。不做forecast scaling，不使用百分比动量。
- 每板块20%风险预算，每周期固定10%；缺失份额闲置，板块或周期之间不转移。各周期独立排名/方向，农业与化工分别取最高正值、最低负值；RB/T/AL取正负号。
- 35日robust EWMA点差波动率换手数；内部目标先形成，再按真实合约净额。流动性上限、252日协方差统一缩放、63日组合实现波动率反馈、保证金/杠杆限制及10% buffer仅用于最终净目标一次。内部抵消不成交、不收费。
- 目标年化波动率27.5%，混合周度，紧急阈值120%；国债豁免、商品限制、次日开盘、拒单与部分成交等原样继承。
- C3客户手续费=历史交易所标准×1.5；正常基础滑点、换月额外tick、参与率冲击与代理费用沿用v4.1。S2仅基础滑点改为3 tick。滑点通过成交价记账一次。

## 3. 必须开发/验证的独立模块

1. v4.2配置、注册及只读输入指纹；不得修改任何旧版本文件。
2. 周度信号/窗口/日历/未来追加审计及G1/G2逐表复现。
3. 两个固定风险袖套的内部目标、真实合约净额、最终一次性约束、真实订单成本审计。
4. 同路径成本加回、账户与成本勾稽、风险及净额统计。
5. 回撤事件、条件/滚动相关、共同尾部、联合移动分块Bootstrap。
6. 独立中文审计/结果/相关性报告、全量CSV+pickle、测试日志和变更清单。

旧袖套实现有内部流动性截断，本轮若前置审计通过需独立实现“仅最终净目标截断”，不能直接把旧实现当作符合本规范。此项是规范指定的净额规则，不是允许修改旧版本。

## 4. 统计设计锁定

主要对象：G1、G2、S1；正式对比加v3的252日参照及S2。扩展周期20/60/120/180仅引用已有正式结果，不新增引擎运行。

成本后日收益=net_pnl/前日权益；同路径成本前日收益=(net_pnl+客户手续费+记录的基础滑点+换月滑点+市场冲击)/前日权益，仅加回一次，不反馈重算未来仓位，不称作零成本回测。

每策略独立计算峰值和DD=equity/running_peak−1。固定状态为DD<0、DD≤−10%、DD≤−20%；事后峰谷恢复事件不作交易信号。保留真实零收益，不把文件缺失填零。

必须输出全样本/样本内/验证期/年度Pearson、Spearman、Kendall；快回撤、慢回撤、同时、任一、恰一回撤条件；等权路径最差5%及1%日；63/126/252完整窗口滚动相关分布及低于0转高于0.5的次数/持续时间。条件共同有效观测不足20时系数缺失并注明。

输出各层级回撤Jaccard、共同回撤天数与比例、另一策略条件平均/累计收益及正日比例、双向峰谷区间保护、共同最差日/周/月、downside beta、co-crash、经验lower-tail dependence、前五大回撤、峰谷恢复日及水下长度。

联合移动分块Bootstrap固定seed=20260902，block=20交易日，重复2000次；全样本、样本内、验证期独立抽样，同一次重复所有策略共用日期区块，不跨阶段边界；每条重采样路径重算回撤和条件状态。保存完整抽样、90%/95%区间及相关差、最大回撤差、重合率、Sharpe差、Calmar差的配对统计。

业绩评价：原始与相同实现风险双重比较；全样本/样本内/验证期、过去5年下一年固定运行、删除2020/2024/同时删除；全部收益/风险/恢复指标、年度与亏损年、FG及年份/板块/品种/多空集中度、成本与真实换手、内部/净目标、buffer/紧急减仓/保证金/杠杆/事后风险贡献。统一披露不完整年度和统计不确定性。

## 5. 判断、交付与边界

严格采用研究规范7.3的十条联合判断，不以全样本最高收益筛选：样本内和验证期正CAGR；验证期及滚动Sharpe不显著差于250日；回撤/水下有经济意义改善且同风险仍成立；删年不结构失效；3 tick验证期为正；净额成本不抵消分散；不集中于FG/单板块/少数年；Bootstrap多数支持并披露跨零区间；全部审计通过。通过也仅列“并行观察候选”，不称干净样本外证明。

任何停止条件触发，则只交付已完成审计、失败证据、测试及未执行项目清单，不能编造袖套/相关性/Bootstrap结果或给出新方案推荐。旧基准不因新研究失败自动证明可靠。

报告与底层文件一一链接，DataFrame均CSV+pickle。保留所有已运行结果，最多4个历史引擎场景；不加参数、不进入备选库或v5。
