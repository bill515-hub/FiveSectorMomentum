# 审计日志（audit_20260903_v2）

> 记录全部勘察/搜索/运行命令、命中数、工具版本、随机种子。工作目录均为 `D:\FiveSectorMomentum`（Git Bash 路径 `/d/FiveSectorMomentum`）。所有写操作仅落在 `audit_20260903_v2/`。

## 0. 环境与工具版本

- OS：Windows 10.0.26200（MINGW64_NT-10.0-26200，Git Bash / GNU coreutils 8.32）
- Python 3.13.8（D:\veighna_studio）；pandas 2.2.3；numpy 2.2.3；scipy（可用）；pyarrow 不可用（故全部数据为 .pkl）
- ruff 0.16.5（`python -m pip install --target audit_20260903_v2/tmp/pylibs ruff`，装于审计目录，未污染仓库/全局环境）
- tushare SDK（仓库 venv 自带，用于跨源重拉，token 取自仓库 .env，未回显）
- 网络：WebSearch（上期所/中金所官方公告核对）
- 种子：审计抽验 `np.random.default_rng(20260903)` / `DataFrame.sample(random_state=20260903)`；框架侧 seed（复核对象）=20260902（bootstrap）、7/4/9（测试）

## 1. 只读边界建立

```
ls -la /d/FiveSectorMomentum
find /d/FiveSectorMomentum -maxdepth 3 -type d -not -path "*/.git/*" -not -path "*/.mimosa/*" | sort
du -sh data outputs src scripts tests docs configs    # 289M/4.1G/1.2M/178K/156K/383K/40K
find . -type f -not -path "./.git/*" -not -path "./.mimosa/*" | wc -l   # 4493
mkdir -p audit_20260903_v2/tmp                        # 唯一新建写边界（既有 audit_20260903/ 只读）
```
发现既有 `audit_20260903/`（前次审计 2 文件）→ 本审计改用新目录 `audit_20260903_v2/`，未触碰前者。

## 2. 哈希快照（零改动证明的基础）

```
# pre（后台任务 exec_e1cea35f，完成 exit 0）
find . -type f -not -path "./audit_20260903_v2/*" -print0 | sort -z |
  while read -d '' f; do sha256sum + stat -c%s; done  > audit_20260903_v2/pre_audit_manifest.txt
# 4758 行哈希；git state: HEAD=093b24a, porcelain: ?? .mimosa/ ?? audit_20260903/ ?? audit_20260903_v2/
# post（后台任务 exec_d729211e，同命令）→ post_audit_manifest.txt（4761 行）
# 差异比对
awk '/=== FILE HASHES/{f=1;next} f&&NF>=3{print $1" "$3}' pre|sort > tmp/pre_hashes.txt
（post 同理）→ diff tmp/pre_hashes.txt tmp/post_hashes.txt
# 结果：差异 4 行，全部位于 ./.mimosa/（插件会话簿记）；src/scripts/tests/configs/data/outputs/docs/.git/根文件 零差异
```

## 3. 源码精读（Read 工具，只读）

全量精读：`settings.py, universe.py, data_source.py, data_pipeline.py, signals.py, signals_v4.py, signals_v4_2.py, calendar_v4_2.py, engine.py, engine_v3.py, engine_v4.py, engine_v4_1.py, engine_v4_2.py, costs_v3.py, workflow.py, workflow_v3.py, workflow_v4.py, workflow_v4_1.py, workflow_v4_2.py, analytics.py, analytics_v3.py, analytics_v4.py, analytics_v4_1.py, analytics_v4_2.py, storage.py, cli.py, __init__/__main__`；`scripts/`（download_v3_fee_data, fetch_calendar_v4_2, audit_v4_2_preflight, correct_v4_1_sleeves, run_v4/run_v4_1/run_v3_tests, run_v4_2_repaired, finalize_* 抽读）；`tests/`（test_core、test_v4_2_preflight 全文 + 其余 5 文件测试清单）；`configs/*.yaml` × 7 全文；docs（V4_2 注册表/日历修复附录全读）。

## 4. 未来函数分类学 grep（仓库根，`--include="*.py"` 覆盖 src/scripts/tests）

| 命令 | 命中 |
|---|---|
| `grep -rn "shift\(\s*-" src/ scripts/ tests/` | 0 |
| `grep -rn "shift(-" ...` | 0 |
| `grep -rn "bfill\|backfill\|interpolate\|fillna(method" ...` | 1（测试方法名，非实现） |
| `grep -rn "center=True\|center = True" ...` | 0 |
| `grep -rn "fit_transform\|StandardScaler\|MinMaxScaler\|zscore\|rank(pct\|rank(axis" ...` | 0 |
| `grep -rn "pct_change(-\|pct_change( -" ...` | 0 |
| `grep -rn "ffill\|fillna(method" src/ scripts/` | 1（data_pipeline.py:179，前向填充，语义安全） |
| `grep -rn "FORWARD" src/ scripts/ tests/`（排除定义行） | 写 1 处（data_pipeline.py:358），读 0 处 |
| `grep -rn "iloc\[-" src/ scripts/` | 38（全部逐条分类，见报告 §2） |
| `grep -rn "seed\|default_rng\|RandomState" ...` | 12（bootstrap seed 20260902 配置+硬编码各半；测试种子 4/7/9） |
| `grep -rn "import \*" ...` | 1（workflow_v4_1.py:9） |
| `grep -rn "outputs/v[0-9_]*_20" src/ scripts/` | 14 处硬编码历史输出目录 |

## 5. 独立复算脚本（均写入并运行于 audit_20260903_v2/tmp/，PYTHONDONTWRITEBYTECODE=1）

1. `data_quality_analysis.py` → `data_quality_report.txt`、coverage_by_instrument.csv、survivorship_by_instrument.csv、mapped_locked_board_days.csv、mapped_days_without_bar.csv、fee_rules_last_per_instrument.csv、random_sample_for_crosscheck.csv
   关键输出：bars 无 limit/margin/fee 列；settlement 非空率 1.0；17 品种 0 缺日/0 错价/0 缺 bar/0 重复；锁板 12 天；v1 stitch_failures=104、v2 0；退市合约在库；配置品种 ⊆ FALLBACK_ECONOMICS。
2. `fee_fill_analysis.py` → `fee_fill_accounting_report.txt`、fills_*.csv
   关键输出：v3/S1 成交滞后 min1/max11/100%>0；平今分段 0；代理费分段 44%/29.4%；逐品种每手费；会计五恒等式独立复算残差 0.000000；v2 兜底费 T 207 元/手。
3. `rb_fee_raw_check.py` → RB 原始费率 0.1（2021-2026 恒定）/0.3（2023 两段）；AL 3、SC 20、T 3（rate 字段）、SP 0.05→0.02；RB 规则区间表。
4. `refutation_materiality.py` → `refutation_materiality_report.txt`
   关键输出：期末权益 v3 55.64M / S1 118.58M / G1 49.29M / G2 68.28M；H2 修正影响 −487k/−1.51M；锁板 7 笔成交方向还原（4 逆）；v4_1 六 sleeve 平今=0；日历年度分布；RB 代理期 2015-2020 规模。
5. `cross_source_check.py` → tushare 重拉 15/15 一致（1 行少 vol 可比项）。
6. `mapping_semantics_check.py` → RB/FG/SC 换月切换日 OI 语义抽查通过。

## 6. 官方公告核对（WebSearch，一手来源）

- 中金所收费一览表（T 3 元/手、平今免收）：http://www.cffex.com.cn/cn/zjssf/20240701/39212.html
- 上期所 2024-05-29（RB 非基准月万0.2）：https://www.shfe.com.cn/publicnotice/notice/202405/t20240529_801782.html
- 上期所 2024-08-26（RB2501 万1）：https://www.shfe.com.cn/publicnotice/notice/202408/t20240826_802176.html
- 上期所 2026-07-08〔2026〕261号（SP 万0.5→万0.2）：https://www.shfe.com.cn/publicnotice/notice/202607/t20260708_832508.html
- 上期发〔2023〕156号（RB2405 万3，2023-05-24 起）：经东海期货等转发文本核对
- 2023-09-05 RB2401→万1（2023-09-07 起）：新浪财经/国盛期货转发

## 7. 静态检查与测试

```
ruff（audit tmp 内）：ruff check --no-cache --statistics src/ scripts/ tests/   # 140 errors（分布见报告）
                     ruff check --no-cache --select E501 src/ scripts/          # 1131
                     ruff check --no-cache --select B023,F841,BLE001,RUF012 ... # 23（B023 8 处经语义核实为误报）
unittest：PYTHONPATH=src;tests PYTHONDONTWRITEBYTECODE=1 python -m unittest discover -s tests -p "test_*.py" -v
          # Ran 66 tests in 58.422s；65 ok / 1 FAIL（test_v4_2_preflight.test_02，框架保留见证）
mypy：环境未安装且项目无类型标注 → 未运行（如实声明）
plugin-review(review_audit)：本会话无该工具 → 未运行（如实声明，以 ruff+精读+测试替代）
```

## 8. 写操作清单（合规性自查）

全部写操作（Write/Edit 工具或重定向）落点：`audit_20260903_v2/`（manifest×2、报告×3、零改动证明、tmp/ 脚本与输出、pylibs/ruff）。另：`pip install --target` 仅写审计目录；两次 Mimosa 钩子对审计脚本的拦截（pickle 反序列化、sed 直写）均已按安全流程改用显式信任边界注释 + Write/Edit 工具提交。**未对仓库任何既有文件执行写入/删除/重命名/移动；未运行任何回测/下载写回原路径**。`.mimosa/` 的 4 行差异为审计插件自身会话簿记，非本人命令产物（见零改动证明）。

## 9. 时间线摘要（本地时间 2026-09-03）

13:37 建目录 → 13:38 pre-manifest（后台）→ 13:40-14:20 源码精读 → 14:20-14:30 分类学 grep → 14:35-14:50 数据质量脚本×2 轮 → 15:00-15:15 官方费率核对（RB/SP/T）→ 15:20 RB 原始费率核查（H2 定案）→ 15:30 反证实验 → 15:40 测试套件 → 15:50 ruff 安装与运行 → 16:00 跨源抽验 + 映射语义 → 16:10 post-manifest → 16:15-16:40 交付物撰写。
