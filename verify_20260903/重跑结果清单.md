# 重跑结果清单（verify_20260903 → 后续重跑计划）

> 依据：`验证总报告_逐条分析.md`、`验证日志.md`、`reports/T1–T8`。
> 原则：**原仓库一切内容只读**；所有重跑产物写新建目录（如 `rerun_20260903/`），零改动。
> 种子与可复现：沿用框架既有 seed 约定；重跑命令与输出完整留痕。

---

## P0 —— 必须重跑（不重跑则当前数字不可引用）

### R1　费率单位修正后的全套正式结果
- **原因**：H2 坐实（`costs_v3.py:48` 应 `/1000` 而非 `/10000`），RB/SP 手续费低估 10 倍。
- **重跑范围**：
  - v3 正式基准（`00_formal__formal_baseline`）+ 全部 `01_fee__*` 场景（5 个）+ `07_break_even_fee__*`（盈亏平衡）；
  - v4_2 `S1__strategy_sleeve_20skip5_250_equal_risk` 与 `S2`（fixed_3tick）；
  - 重出：净利、Sharpe、最大回撤、费用盈亏平衡倍数（预期 ~54× → ~52×）、逐日 equity/commission。
- **验收**：重算后手续费较现值上升 ≈ +47万（v3）/ +140万（S1）量级；净利相应下调；结论方向（正收益）不变。

### R2　锁板拒单生效后的重跑（幽灵成交剔除）
- **原因**：H1 坐实——`reject_locked_limit` 从未生效（bars 无 `upper/lower_limit` 列），锁板日逆方向成交 4 段（全口径 14 段）现实大概率不成交。
- **前置**：补齐停板价来源（见修改方案 M3）：`pre_settle × 交易所停板幅度` 推算，或获取高权限 `ft_limit`。
- **重跑范围**：v3 基准 + v4_2 S1；重点复核 **2025-04-07 RU2505 换月旧腿逆跌停卖出（278 手）**。
- **验收**：逆停板成交被拒单；逐笔拒单清单与 `rejections` 对齐；净利/回撤在极端日（2020-02-03、2021-10-08、2025-04-07）下调。

### R3　真实逐日保证金率下的约束重估
- **原因**：H1 保证金静态兜底×1.25 使约束**过度保守**；真实 `long/short_margin_rate` 已在 `data/v3/fees/tushare_fut_settle_daily_raw.pkl`（35,742/35,873 非空，T 需 ÷100 归一）。
- **重跑范围**：用真实逐日率替换静态率，重算占用约束，量化"约束变松后持仓/换手/收益是否变化"。
- **验收**：触发日显著下降（T3 已示 v3 商品触发 67→2、总 86→2）；确认结论方向是"原结果保守"而非"风险低估"。

---

## P1 —— 建议重跑（补全本轮未覆盖的验证盲区）

### R4　信号生成层独立复算
- **原因**：T8 以 `orders.pkl` 为外生输入，未重算评分/周选择/eligibility；这是唯一未被打到金标准的核心链路。
- **做法**：从原始 bars + 配置独立实现 v4_2 的评分→周选择→目标，与框架 `scores/selections/targets` 对齐。
- **验收**：信号方向/入选品种与框架一致；不一致项逐条归因。

### R5　M1（v1/v2 费用兜底高估 ~57 倍）独立重算
- **原因**：本轮未重算，沿用原审计数据。
- **做法**：独立重算 v1/v2 的 T 逐手费用（207 元 vs 官方 3.6 元），并标注 v1/v2 成本结论作废或修正。

### R6　M2/M3 方法学条目量化（验证期择优 + 伪 walk-forward）
- **原因**：属逻辑审查型结论，本轮未做重训实验。
- **做法**：① 冻结样本外期（如 2024+）只读一次重估胜者显著性；② 真 walk-forward（滚动窗口内重选参数）对比切片展示，量化结论偏差。

### R7　Panama 百分比口径的下游清理
- **原因**：新发现——Panama 复权序列水平漂移使百分比收益失真（RB 2016-11-29 "-23%"），框架点差信号自洽，但下游任何 `pct_change` 用法无效。
- **做法**：检索仓库/文档中所有对 adjusted_prices 做百分比收益的地方，逐一改点差或同号率，或加显式警示。

---

## 重跑执行约定
1. 每个重跑先跑**校准门**：重算结果必须与"被验证为正确的现状"在对应维度对齐（例：R1 前先确认 /10000 口径复算 = 现值，再切 /1000）。
2. 记录 seed、命令、diff 摘要；产物写 `rerun_20260903/`，绝不覆盖 `outputs/`。
3. 每个 P0 项出"修正前 vs 修正后"对照表（净利/Sharpe/回撤/费用/触发日），明确方向与量级。
