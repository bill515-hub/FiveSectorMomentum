# 独立二次验证日志（verify_20260903）

> 角色：独立复核工程师。方式：换实现（不 import `five_sector_momentum.*`、不复用 `audit_20260903*/tmp/*`）+ 换数据源（akshare 新浪、tushare 重拉、交易所公告）。全程只读仓库，唯一写边界 `verify_20260903/`。

## 0. 环境与工具

- Windows 10.0.26200 / Git Bash（GNU coreutils 8.32）；Python 3.13.8（D:\veighna_studio）
- pandas 2.2.3 / numpy 2.2.3；tushare 1.4.24（已有）；akshare 1.18.8（已有，无需安装）
- 函数存在性预检（任务要求）：`hasattr(ak, ...)` → tool_trade_date_hist_sina: OK / futures_zh_daily_sina: OK / futures_main_sina: OK / futures_comm_info: OK（未 blocked 项）
- 随机种子：统一 `np.random.default_rng(20260903)` / `random_state=20260903`
- token：从 `ROOT/.env` 读取，全程未回显
- Python 运行一律 `PYTHONDONTWRITEBYTECODE=1`；哈希用 `sha256sum`

## 1. 只读边界与哈希

```
pre_manifest.txt / post_manifest.txt：
  find . -type f -not -path "./.git/*" -not -path "./.mimosa/*" -not -path "./verify_20260903/*" |
  sort | sha256sum+size 逐文件
  覆盖 pre: 4756 文件（含 .env；不含 .git/.mimosa/verify_20260903）
git 状态两快照一致：HEAD=093b24a，porcelain 仅 ?? .mimosa/ ?? audit_20260903/ ?? audit_20260903_v2/ ?? verify_20260903/
```
差异判定见 `零改动证明.txt`。

## 2. 任务执行记录（脚本均在 tmp/，报告在 reports/）

| 任务 | 脚本 | 关键命令/输出 |
|---|---|---|
| T1 | tmp/t1_fee_unit_audit.py | fee raw 36,040 行→usable 35,873；特例识别：CFFEX & fee缺/0 & rate>0 → **{T}**（rate 字段 distinct=[3.0]）；真费率型（rate>0 & fee缺/0 & 非CFFEX）→ **{RB, SP}**；RU(固定3元)正确排除=True。RB rate 生效区间 0.1(2021-01-04..2026-08-31)/0.3(2023-09-05..12-08)；SP 0.05(2021-01-04..2026-07-07)/0.02(2026-08-31)。逐 fill 独立重算（/1000 与 /10000 双口径）→ fee_impact_recompute.csv |
| T2a | tmp/t2a_endpoint_diag.py | fut_settle 单合约 RB2410=103行 ok / MA401=0行 / T2403=9行；**批量6合约逗号调用=0行（无异常）**；ft_limit 单/批均 ERROR 无权限 → t2a_endpoint_diagnostics.csv |
| T2b | tmp/t2b_locked_replay.py | 锁板日(主力映射, high==low & vol>0)=**12 天**；锁板日成交 v3=1、S1=6（共 7 段）；逆停板方向 **4 段** → locked_board_days.csv / locked_fill_replay.csv |
| T2补 | tmp/locked_reconcile.py | 口径对账：任意合约锁板日成交=17 段/逆 14；其中非映射合约 10 段全部为 2025-04-07 RU2505 换月旧腿跌停卖出 → locked_scope_reconciliation.csv |
| T3 | tmp/t3_margin_sensitivity.py | long_margin_rate 非空 35,742/35,873；S0 复算 vs 记录 margin/commodity_margin **max|diff|=0.000000（两 run）**；S3 真实率（T 百分数 >1.0 归一 /100，命中 258 行）覆盖 63% 持仓日（回退 6,885/18,611 与 5,733/21,394）→ margin_scenario_utilization.csv |
| T4 | tmp/t4_offline_bundle.py | 会计：逐日/净=毛-费/期末恒等 最大残差 3.7e-9、8.4e-9（浮点级）与 0；平今计数仅 {open, close_non_today}；覆盖：mapping 45,773 日缺 bar/零量=**0**；退市：fut_basic 10,988 distinct、已退市 10,118、退市且在 daily 2,379/2,478；v1 stitch_failures **len=104**（抽样 5 例我的重叠日检索均=0，定义一致）；锁板交叉（实现B）→ recompute_locked_crosscheck.csv + 口径对账（见 T2补） |
| T5 | tmp/t5_calendar_cross.py | 新浪日历 8,797 行；对比区间裁剪至仓库日历覆盖（2014-01-02..2026-12-31）：新浪 3,161 vs 仓库 3,161 开市日，**双向差异 0 天**；锚点 2020-02-03 开/2020-01-31 休/2020-10-01 休/2021-02-11 休全部正确 → calendar_diff.csv |
| T6 | tmp/t6_price_cross.py | 5 合约×20 日（rng=20260903）抽样，新浪匹配 69/100 天（SC2409 新浪仅存续末期 10 天、MA409 仓库 0 行跳过）；**open/high/low 69/69 全部 0 差**；close 67/69 零差（RB2410 2024-09-25 差8点、AL2406 2023-08-24（当日全天才 13 手）差 40 点，均为流动性/收盘口径边缘）；settle 差异全部因新浪报 0（缺值），仓库结算价从未被矛盾值反驳 → price_diff.csv |
| T7 | tmp/t7_mapping_rebuild.py + t7b_sina_main.py | 最大 OI 重建映射：**43,574/45,773=95.20% 逐日一致**；不一致 2,199 天 = tushare_fut_mapping 2,076（官方映射量仓混合规则早于纯 OI 切换）+ 到期保护 123 → mapping_diff*.csv。新浪主力连续：T corr 0.961/同号 98.37%、SC corr 0.930/97.95%、RB 同号 91.38%（corr 失真诊断见 T7b 附录：新浪未复权换月跳变 + Panama 低水平百分比放大 2016-11-29 -23% 为固有属性非数据错误）→ main_series_crosscheck.csv |
| T8 | tmp/t8_reference_engine.py | 纯自研执行内核（订单流外生）：执行级对比 **3460/3460 数量一致且价格一致（最大差 0）**；配执行级费用后逐日权益 **2834/2834 天差异 0.00 元、期末权益分毫不差 55,639,975.35**；费用代入差异时权益末端差 -25,353.52 元（=T1 费用口径差的体现）→ reference_fills_diff.csv（0 行差异）/ reference_equity_diff.csv（全 0） |

## 3. 随机性
- 抽样种子 rng=20260903（T4 拼接抽样、T6 价格抽样）。
- 复核对象自身的随机性（bootstrap seed 20260902 等）不在本次验证范围（验证为确定性复算）。

## 4. Blocked 项
- 无。akshare 四函数全部可用；tushare token 可用；全部 T1–T8 完成。
- 唯一环境注记：mypy/`plugin-review` 不属于本任务清单；未使用。

## 5. 结论判定表

| 被验审计结论 | 判定 | 证据 |
|---|---|---|
| H2 费率单位 /10000 应为 /1000（RB/SP 低估10倍），v3 +48.7万 / S1 +150.6万 | **坐实（数量级一致，细节修正）** | 特例={T}、费率型={RB,SP} 与预期完全一致；我的独立重算（raw 逐日映射 + 最新标准代理）得 v3 **+472,609**、S1 **+1,397,694**（略低于审计值，因审计用框架区间压缩规则列、我用朴素最新标准回填——/10000 口径下两者差 -31k/-142k 即此因）；方向与 10 倍口径被三组官方公告锚点 + 独立重算双重确认 |
| H1 fut_settle/ft_limit 0 行零告警、涨跌停拒单从未生效 | **坐实且根因细化** | T2a：fut_settle 批量调用静默 0 行（单合约 103 行证明端点可用）；ft_limit 无权限；manifest warnings=[] 与"空返回不触发告警"分支吻合 |
| H1 锁板 12 天/成交 7 段/逆停板 4 段 | **精确复现 + 影响面上修** | T2b 独立实现得 12/7/4；扩展口径（任意合约锁板日）另有 2025-04-07 RU2505 换月旧腿跌停卖出 174 手（10 段全逆方向）——审计未计入该口径，H1 实际影响略大 |
| H1 保证金退化静态兜底率×1.25 | **坐实（零残差）** | T3 S0 复算 vs 记录值 max|diff|=0.000000；且 S3 真实逐日率（T 百分数归一后）显示真实占用约束更松（v3 触发日 2 vs 67/86）——该失真方向为"过度约束"而非风险低估 |
| 会计勾稽零误差 | **坐实** | T4 独立核算最大残差 ≤8.4e-9（浮点级）；T8 全路径权益 0.00 差 |
| 平今=0（T/SC 平今代理无实际影响） | **坐实** | T4：两 run transaction_type 仅 {open, close_non_today} |
| v2+ 主力序列日历覆盖 100% / 映射日缺 bar 0 | **坐实且换源确认** | T4 覆盖 0/45,773；T5 新浪日历双向 0 差异 |
| 退市合约在库（无幸存者截断） | **坐实** | T4：2,379 已退市合约在 fut_daily（共 2,478） |
| v1 拼接失败 104 次 | **坐实** | T4：len=104；抽样 5 例重叠日检索均为 0（定义吻合） |
| tushare 价格数据可靠（审计用同源重拉） | **换源增强** | T6：新浪 69 匹配样本 O/H/L 全零差；close 2/69 边缘差已逐日解释 |
| 主力映射合理（tushare 官方映射） | **换源增强** | T7：95.20% 与纯最大 OI 一致，差异全部可归因（官方混合规则/到期保护）；新浪主力同号率 91-98% |
| 执行时序 t 收盘→t+1 开盘、滑点/参与率/盯市模型如代码所述 | **金标准坐实** | T8：3460/3460 执行数量+价格逐笔一致；权益路径 0.00 差 |
| （审计未主张）Panama 序列上的百分比收益分析 | **新发现（警示）** | T7 附录：回溯调整后水平漂移会放大百分比波动（RB 2016-11-29 -23% 为固有属性）；框架点差信号设计自洽，但任何下游若做百分比分析需换回原始/比价口径 |

## 6. 复现/证伪/blocked 总览
- **复现（含精确复现与量级复现）**：H1（含根因+影响面上修）、H2、锁板 12/7/4、会计零误差、平今=0、覆盖 100%、退市在库、拼接 104、价格/日历/映射三源交叉全部通过。
- **证伪**：无（未发现原审计结论性错误；两处数量级内差异均已归因：T1 的 +47.3/139.8万 vs +48.7/150.6万 为费用规则压缩差异；T7 RB 相关性失真为口径问题非数据问题）。
- **修正/补充**：H1 锁板影响面（+10 段逆方向换月旧腿）；H1 保证金方向（过度约束而非低估）；新增 Panama 百分比收益无效警示。
- **blocked**：无。

## 7. 过程事件与披露（诚实记录）

1. **pre_manifest 首次生成失败**：任务开始时的后台快照因 shell cwd 漂移（会话早期曾 `cd` 至 audit_20260903_v2）导致重定向 `verify_20260903/pre_manifest.txt` 失败（"No such file or directory"，实际 0 行落盘，任务仍 exit 0）。发现于收尾 diff。处置：以绝对路径重建 pre_manifest.txt（头部注明"任务完成后重建"），零改动结论改由**三方比对**支撑：① `audit_20260903_v2/post_audit_manifest.txt`（2026-09-03T08:1xZ，早于本验证会话开始≈13:45Z，覆盖同一批仓库文件）② 本目录 `post_manifest.txt`（22:37Z）③ 重建 pre（22:4xZ）。三者在"仓库本体"（剔除 .git/.mimosa/audit_*/verify_*）路径集上必须逐文件一致，方可证明本会话零改动。详见 `零改动证明.txt`。
2. **目录内出现非本人创建文件**：`verify_20260903/二次验证与数据源方案.md`（17,242 字节，21:59 出现）为用户方放入的本任务方案文档（内容与本任务清单同源，T1–T9 结构）。本人未创建、未修改该文件；它在写边界内且不属于哈希范围（verify_20260903 被排除），不影响零改动证明。已完成任务 T1–T8 与该方案 T1–T8 对应关系一致。
3. **脚本修正记录**（全部在审计目录内自查发现并修正）：T3 曾因费率表键序 (ts_code,trade_date) 与持仓表 (date,contract) 写反导致 S3 全回退（修正后真实率覆盖 63%）；T3 曾发现并归一 CFFEX(T) 保证金率百分数单位漂移（rate>1.0 判为百分数 ÷100，命中 258 行）；T5 曾因对比区间含新浪 1990 年起历史造成假差异（裁剪至仓库日历覆盖区间后 0 差）；T6 新浪列名为英文 date/open/...（适配）；T7b 新浪主力列名为中文（适配）；T8 首轮以分段级对比误判数量差（改为执行级聚合后 3460/3460 全一致）。
4. **网络调用**：tushare 7 次（T2a）、akshare 新浪：日历 1 + 单合约日线 4(成功)/1(0行跳过) + 主力连续 3 + 若干探测；均只读，不落任何缓存于仓库。
