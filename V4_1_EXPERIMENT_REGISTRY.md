# v4.1 实验注册表：成本、周期影响与策略分散

> 状态：**已锁定**  
> 创建日期：2026-09-02  
> 创建时点：早于任何 v4.1 新回测、袖套结果、成本反事实结果或 Bootstrap 结果  
> 唯一规范：`docs/V4_1_COST_SIGNAL_DIVERSIFICATION_TASK.md`  
> 研究边界：只执行 v4.1，不执行 v5；v2、v3、v4 代码、配置、缓存、注册表和结果只读

## 1. 冻结基准

除下文明确列出的成本反事实外，全部场景固定继承 v4：交易所历史手续费×1.5、分品种正常基础滑点、换月每腿额外 1 tick、滞后成交量参与率冲击、混合周度、10% buffer、120%实现波动率紧急减仓阈值、27.5%目标波动率、商品保证金/杠杆约束、国债杠杆豁免、原流动性过滤、主力映射、Panama、t 日收盘形成信号及下一交易日开盘成交。信号、波动率均使用绝对点差。

换手继续定义为：`成交名义金额总和 / 样本平均权益 / (观测交易日/252)`。

同路径成本前盈亏固定为：`净盈亏 + 客户手续费 + 基础滑点 + 换月额外滑点 + 市场冲击`。滑点分项由记录的总滑点金额按 tick 分量拆分，只加回一次。该曲线冻结原持仓和成交手数，不反馈未来仓位。

## 2. 新增模块

- `configs/five_sector_momentum_v4_1.yaml`：只增加 v4.1 注册参数；冻结字段复制自 v4。
- `src/five_sector_momentum/engine_v4_1.py`：继承 v4 执行，增加逐成本组件开关和固定袖套目标；不回写 v4。
- `src/five_sector_momentum/analytics_v4_1.py`：同路径成本、成本响应、forecast/选择/收益相关、分散、袖套净额和 Bootstrap。
- `src/five_sector_momentum/workflow_v4_1.py`：复现闸门、固定运行顺序、53次运行硬上限、保存与勾稽。
- `src/five_sector_momentum/reports_v4_1.py`：三份中文报告。
- `tests/test_v4_1.py`、`scripts/run_v4_1.py`：新增测试与唯一入口。

## 3. 固定执行顺序与运行数量

### 阶段 0：四场景复现闸门（4 次新增引擎运行）

依次运行正式成本：

1. `gate_reference_v3_252`
2. `gate_single_180_skip5`
3. `gate_multi_all_raw`
4. `gate_multi_fast_scaled`

分别与 v4 的对应场景比较 forecast、selections、directions、equity、targets、positions、orders、fills、transaction_type、各滑点分项、手续费、板块/品种盈亏、换手和期末权益。按业务主键排序逐值比较，金额容差 0.01 元。任一失败立即停止，不运行阶段 1—4，并生成失败审计文件。

### 阶段 1：17候选同路径成本加回（0 次引擎运行）

固定对象为 v4 阶段1—3的17个候选，不含参照、留一诊断和压力场景。全部读取原 v4 成交和账户路径，输出净/成本前曲线、三阶段指标、成本分项、年度由正转负、单位换手和单位成交成本。

### 阶段 2：有限成本反事实矩阵

固定代表信号顺序：

1. `reference_v3_252`
2. `single_20`
3. `single_60`
4. `single_120`
5. `single_180_skip5`
6. `single_250_minus_20`
7. `multi_all_raw`

固定成本点顺序和定义：

|代码|手续费|基础滑点|换月额外滑点|参与率冲击|
|---|---:|---:|---:|---:|
|C0|0|0|0|0|
|C1|交易所×1.5|0|0|0|
|C2|交易所×1.5|正常基础 tick 减1，最低0|1 tick|原阶梯|
|C3|交易所×1.5|正常基础 tick|1 tick|原阶梯|
|C4|交易所×1.5|正常基础 tick 加1|1 tick|原阶梯|
|C5|交易所×1.5|正常基础 tick 加2|1 tick|原阶梯|
|C6|交易所×1.5|全品种2 tick|1 tick|原阶梯|
|C7|交易所×1.5|全品种3 tick|1 tick|原阶梯|

56个结果单元全部保留。C3不重复运行：四个闸门对象引用通过闸门的 v4.1 结果，其余三个引用已复核 v4。完全同参数的 C6/C7 也不重复运行：`reference_v3_252`引用 v3 的 `break_even_tick_2/3`，`single_180_skip5`和`multi_all_raw`引用 v4 已有 fixed2/fixed3。其余固定为43次新增反事实引擎运行。C0仍保留次日成交、涨跌停拒单、参与率上限、部分成交、换月和风险约束。

不运行 Shapley 账户回测，不增加成本点，不外推未发生符号穿越的盈亏平衡点。

### 阶段 3：三个策略袖套（6 次新增引擎运行）

固定顺序：

1. `strategy_sleeve_fast_equal_risk` 正式成本
2. `strategy_sleeve_fast_equal_risk_fixed_3tick`
3. `strategy_sleeve_all_equal_risk` 正式成本
4. `strategy_sleeve_all_equal_risk_fixed_3tick`
5. `strategy_sleeve_slow_equal_risk` 正式成本
6. `strategy_sleeve_slow_equal_risk_fixed_3tick`

周期与每板块风险份额：fast=(20,60,120)，每周期20%/3；all=(20,60,120,250)，每周期5%；slow=(60,120,180,250)，每周期5%。每周期独立方向和横截面选择。无信号份额留空，不转移。内部目标先按真实合约代数净额；组合协方差/实现波动率反馈、保证金/杠杆和10% buffer仅对最终净目标一次；只有真实净订单收费。保存净额前内部目标和净额后真实目标。固定3 tick只改变基础滑点。

袖套净额节省分两层报告：一是净额前内部目标绝对手数与代数净额目标的确定性差；二是根据真实费率、tick价值和注册成本组件估算的“未净额虚拟交易成本”。虚拟成本明确标为代理，不进入账户，不冒充真实成交。

### 阶段 4：联合移动分块 Bootstrap（0 次引擎运行）

固定21个基础策略：1个`reference_v3_252`、17个v4候选、3个正式成本袖套。排除留一诊断、所有压力/成本响应和同路径加回曲线。正式成本后收益与同路径成本前收益分别处理。

- 随机种子：20260902；移动分块：20交易日；重复：2000。
- 阶段：全样本、样本内、验证期；各阶段独立合法区块，不跨边界。
- 每次重复的21策略共享完全相同的区块起点及顺序；末块截断。
- 统一正式交易日历；真实0收益保留，缺失账户日直接报错，不填0。
- 每条重采样路径重新计算 CAGR、波动率、Sharpe、Sortino、最大回撤、Calmar、累计收益。
- 保存完整区块抽样明细、252,000行策略抽样指标、配对差值、名次及稳定性摘要。

## 4. 分析定义

### 4.1 周期影响

只在每周独立信号形成日统计。对 fast/all/slow 的 raw/scaled：名义权重、`abs(w f)/sum(abs(w f))`、留一 sign flip、winner/loser与Jaccard变化、决定性周期、同向/反向/抵消，以及留一账户目标、成交、成本、净利润、回撤差。缺任一注册周期时整组缺失，不动态改权。

### 4.2 相关性

- forecast：按品种、阶段、年份、板块计算 Pearson、Spearman、符号一致和零附近分歧；零附近固定为任一 forecast 绝对值≤0.25。只用成对共同有效周度观测。
- 选择：农产品、化工能源周度合格截面 Spearman、最强/最弱重合、选择集合 Jaccard。
- 收益：20/60/120/180/250正式单周期每日成本前/后，按全样本、样本内、验证期、年份、板块和多空计算 Pearson/Spearman；尾部定义为周期等权组合收益最低5%的共同日；回撤重合为两策略同时低于各自历史高水位的共同日占比。
- 分散：等权分散比、相关矩阵有效特征值数量、组合/单周期平均波动率、回撤改善、收益损失和额外成本。

### 4.3 成本弹性与边界

成本弹性只报告实际运行点之间的变化。沿“正常基础tick平移”轴使用C2(-1)、C3(0)、C4(+1)、C5(+2)；固定2/3 tick单列。盈亏平衡仅在相邻实际点符号穿越时线性插值；否则报告未达到及已验证边界。C0到C3的改善拆为同路径会计加回与权益—仓位反馈残差。

### 4.4 推荐规则

不按全样本最高收益选方案。依次检查样本内与验证期、滚动拼接、删除2020/2024、固定3 tick、集中度与FG、收益—回撤交换、成本是否抵消分散、相邻周期证据、未来函数及账户审计。很小变化不称为实质改善。最终只允许：保留v3；`single_180_skip5`作为并行挑战者；某一个固定袖套作为下一阶段观察候选。若没有清晰综合改善，结论固定为“没有足够证据替换现有方案”。

## 5. 总运行数和硬停止条件

- 复现闸门：4次。
- 成本反事实新增：43次；另13个单元引用已复核结果（7个C3、6个固定2/3 tick）。
- 袖套：6次。
- **新增引擎运行总数固定为53次，不得超过；Bootstrap和纯分析不计入。**

任一复现闸门失败、运行身份重复、场景数超过53、Bootstrap策略不等于21、共享区块检查失败、账户勾稽失败或缺失账户日时立即停止并保留失败输出。注册后不得新增窗口、权重、品种、成本点、Bootstrap参数或袖套。

## 6. 固定输出

输出规范要求的三份报告、场景/来源/运行计数表、四场景复现、17候选同路径成本、56单元成本矩阵、盈亏平衡/弹性、成本与换手来源、周期forecast/选择/目标影响、三层相关性、分散指标、6袖套结果及净额归因、21策略Bootstrap全量明细/汇总/配对/排名、年度/板块/品种/多空、buffer/保证金/杠杆、账户勾稽、测试、文件清单和限制。所有DataFrame同时保存CSV和pickle，每张报告表注明底层文件。
